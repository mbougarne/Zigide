---
schema_version: 1
record_id: 1f339216-c9e9-4e79-8564-3a8025b16ed0
created_at: 2026-08-23T19:27:10Z
source_conversation: ../conversations/2026-08-23-19-27-10-1f339216-c9e9-4e79-8564-3a8025b16ed0.md
---

# Commands: Command Inventory, Research Backfill, and ZIT-001

Unique commands used while answering this prompt, deduplicated per the rules in [agents/README.md](../README.md). Outcomes live in the conversation record, not here.

- `git status --short` — see staged versus committed state before touching records
- `git log --oneline` — confirm which trace records were already committed
- `git ls-tree -r --name-only HEAD` — list committed files to separate immutable ledger entries from amendable staged ones
- `git diff --cached` — read the staged governance changes made in another session
- `grep` — locate Milestone 0 exit tickets in the backlog and exit conditions in the roadmap
- `date -u` — generate the UTC timestamp for this record set
- `uuidgen` — generate the record UUID
- `zig build check` — run the repository checks after the changes
- `npx ajv-cli validate` — validate the trace ledger against its JSON Schema
