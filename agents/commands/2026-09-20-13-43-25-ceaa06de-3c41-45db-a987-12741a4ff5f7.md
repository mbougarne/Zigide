---
schema_version: 1
record_id: ceaa06de-3c41-45db-a987-12741a4ff5f7
created_at: 2026-09-20T13:43:25Z
human_actor: project-maintainer
agent: Codex (GPT-5)
source_conversation: ../conversations/2026-09-20-13-43-25-ceaa06de-3c41-45db-a987-12741a4ff5f7.md
---

# Commands: Implement ZIT-003 Through ZIT-007

- `rg` — search memory and repository files for current ticket and architecture guidance
- `sed` — read contributor rules, architecture records, tickets, build configuration, and Zig standard-library APIs
- `git status` — inspect the worktree before changes
- `git branch` — identify the active ticket branch
- `git log` — inspect recent repository history
- `zig env` — inspect the installed Zig toolchain configuration
- `zig version` — verify the pinned Zig toolchain
- `uname -m` — record the host architecture
- `sw_vers` — record the macOS version
- `zig fmt` — format build, source, and test files
- `zig build` — build and install the product executable
- `zig build run` — run the minimal executable
- `zig build unit-test` — run product unit tests separately
- `zig build integration-test` — run product integration tests separately
- `zig build test` — run repository, product, integration, and module-boundary tests
- `zig build check` — run all tests, formatting, hygiene, links, and trace checks
- `git diff --check` — check patch whitespace
- `git diff --stat` — summarize the implementation diff
- `jq empty` — validate the JSON history ledger syntax
- `date -u` — generate the trace timestamp
- `uuidgen` — generate the trace UUID
- `tr` — normalize the UUID to lowercase
