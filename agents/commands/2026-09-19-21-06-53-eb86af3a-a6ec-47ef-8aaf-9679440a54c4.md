---
schema_version: 1
record_id: eb86af3a-a6ec-47ef-8aaf-9679440a54c4
created_at: 2026-09-19T21:06:53Z
human_actor: project-maintainer
agent: Codex (GPT-5)
source_conversation: ../conversations/2026-09-19-21-06-53-eb86af3a-a6ec-47ef-8aaf-9679440a54c4.md
---

# Commands: Align Automatic Pull-Request Review Files

- `rg`, `find`, and `sed` — locate and inspect repository rules, CI, toolchain pins, target files, and trace templates
- `git status`, `git branch`, `git log`, and `git remote` — establish checkout state and repository identity without changing Git state
- `git ls-remote` — resolve action releases to immutable commits
- `apply_patch` — add review configuration and required trace files
- `ruby` YAML checks — parse workflows and mirror skill-frontmatter structural validation
- `go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12` — validate all four workflow files with a pinned actionlint version
- `shasum -a 256` — confirm all repositories use the same orchestration workflow
- `git diff --check` — verify patch whitespace
- `date -u` and `uuidgen` — generate this record's UTC timestamp and lowercase UUID
- `jq empty` — validate collaboration-history JSON
- `zig build check` — run the repository's native hygiene, trace, link, formatting, test, and build gate after record creation
